import { MathsSymbolPipe } from './maths-symbol.pipe';

describe('MathsSymbolPipe', () => {
  it('create an instance', () => {
    const pipe = new MathsSymbolPipe();
    expect(pipe).toBeTruthy();
  });
});
